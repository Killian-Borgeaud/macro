# Full Worked Lecture Example

**Stub.** Will be filled after the first real Macro lecture passes the audit cleanly. The first successful `outputs/lectures/*.html` becomes the canonical reference for what good end-to-end output looks like.

Until populated, rely on:
- The complete worked section in `SKILL.md` under `<payload_schema>` (Solow mechanics section, ~700 words, one figure)
- The full rule bad/good examples in `references/rules.md`
- The 6 SVG templates in `references/svg.md`

If you're reading this during a generation run and feel uncertain about end-to-end shape, that's a signal to ask the user for a quick check rather than guess.
